{% doc %}
  Renders a footer menu bottom snippet for the footer section

  @example
  {% render 'n-footer-cart-drawer-upsells' %}
{% enddoc %}

<ul>
  {% for link in linklists['main-footer-bottom-menu'].links %}
    <a href="{{ link.url }}" aria-label="{{ link.title | escape }}">{{ link.title }}</a>
{% endfor %}
</ul>