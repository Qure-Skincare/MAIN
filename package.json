{
  "name": "shopify-theme",
  "version": "1.0.0",
  "scripts": {
    "dev": "shopify theme dev --live-reload=hot-reload --theme-editor-sync --force",
    "check": "shopify theme check",
    "push": "git add .; git commit -m \"push\"; git push origin feature/$npm_config_b"
  }
}