{% doc %}
  Renders a blocks section.
{% enddoc %}

{% content_for 'blocks' %}

{% schema %}
{
   "name": "t:sections.blocks.name",
   "blocks": [
      { 
         "type": "@theme"
      }
   ],
   "settings": [

   ],
   "presets": [
      {
      "name": "t:sections.blocks.name"
      }
   ]
}
{% endschema %}